const code = "HTML";

switch(code){
    case "HTML":
        console.log(`拡張子は${code}です`);
        break;
    case "CSS":
        console.log(`拡張子は${code}です`);
        break;
    case "JavaScript":
        console.log(`拡張子は${code}です`);
        break;
    default:
        console.log("わからないファイルです");
}